fastapi==0.115.0
uvicorn[standard]==0.30.6
sqlalchemy==2.0.35
pydantic==2.9.2
pydantic-settings==2.5.2
python-dotenv==1.0.1
requests==2.32.3
python-multipart==0.0.12
pyarrow==17.0.0
openai==3.14.1
# Windows에는 OS 시간대 DB가 없어 ZoneInfo("Asia/Seoul")이 ZoneInfoNotFoundError로 죽는다
# (chat_ai.py의 KST). 리눅스·맥은 OS에 있어서 안 깔아도 되지만, 순수 데이터 패키지라
# 같이 설치해도 영향이 없다.
tzdata==2026.4
Pillow==11.0.0
